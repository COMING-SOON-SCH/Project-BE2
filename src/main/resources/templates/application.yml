spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    # hibernate:
    #  ddl-auto: update # ?????´ë?? ?????? ?????? ë°? ?????°ì?´í??
    show-sql: true # SQL ì¿¼ë¦¬ ë¡?ê·? ì¶????
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true
    datasource:
      url: jdbc:h2:mem:testdb
      username: sa
  h2:
    console:
      enabled: true
